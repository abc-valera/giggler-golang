# Instructions for Claude Code

## Version Control

- **Do not** execute any git modification commands (add, commit, restore, push, etc.)
- Only use read-only git commands if needed (status, diff, log)
- I will manage version control manually
